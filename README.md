# Execute this command before running. This will install all dependecies for project from requirements.txt

pip install -r requirements.txt


# Also change the password for MySQL server connection in app.py
